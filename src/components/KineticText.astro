---
/**
 * Kinetic headline — chars/words split for GSAP.
 * Visual spans are aria-hidden; accessible name stays on the heading.
 *
 * Char mode animates per glyph, wrapped in nowrap word groups so line
 * breaks never land mid-word.
 */
export interface Props {
  text: string;
  as?: 'h1' | 'h2' | 'h3' | 'p' | 'span';
  class?: string;
  mode?: 'chars' | 'words';
  when?: 'load' | 'scroll';
  scramble?: boolean;
}

const {
  text,
  as: Tag = 'h1',
  class: className = '',
  mode = 'chars',
  when = 'scroll',
  scramble = false,
} = Astro.props;

const tokens = text.split(/(\s+)/).filter((p: string) => p.length > 0);

let unitIndex = 0;
---

<Tag
  class:list={['kinetic', className]}
  data-kinetic={mode}
  data-kinetic-when={when}
  data-scramble={scramble ? 'true' : undefined}
  aria-label={text}
>
  <span class="kinetic__visual" aria-hidden="true">
    {
      tokens.map((token: string) => {
        if (/^\s+$/.test(token)) {
          return <span class="kinetic__space">&nbsp;</span>;
        }

        if (mode === 'words') {
          const i = unitIndex++;
          return (
            <span class="kinetic__word" style={`--i: ${i}`}>
              <span class="kinetic__inner" data-text={token}>
                {token}
              </span>
            </span>
          );
        }

        const chars = Array.from(token);
        return (
          <span class="kinetic__word-group">
            {
              chars.map((ch: string) => {
                const i = unitIndex++;
                return (
                  <span class="kinetic__char" style={`--i: ${i}`}>
                    <span class="kinetic__inner" data-text={ch}>
                      {ch}
                    </span>
                  </span>
                );
              })
            }
          </span>
        );
      })
    }
  </span>
</Tag>
